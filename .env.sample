# -----------------------------------------------------------------------------
# Core Language Model Configuration
# -----------------------------------------------------------------------------
# Used by LangChain and other components for generative AI capabilities.
# The backend URL can be pointed to OpenAI or a compatible local provider.
OPENAI_API_KEY="sk-..."
BACKEND_URL="https://api.openai.com/v1"

# -----------------------------------------------------------------------------
# On-Chain Data Provider API Keys
# -----------------------------------------------------------------------------
# These keys are required for the OnChainAnalyst to fetch data from various
# blockchain analytics platforms. The system has a fallback to mock data
# if these are not provided, but real-time analysis will be disabled.

# Finnhub API Key
FINNHUB_API_KEY=""

# Get your Glassnode API key from https://studio.glassnode.com/
GLASSNODE_API_KEY=""

# Get your IntoTheBlock API key from https://www.intotheblock.com/
INTOTHEBLOCK_API_KEY=""

# Get your Dune Analytics API key from https://dune.com/
DUNE_API_KEY=""

# -----------------------------------------------------------------------------
# Redis Cache Configuration
# -----------------------------------------------------------------------------
# The application uses Redis for caching to improve performance and reduce
# API call frequency. If not configured, caching will be disabled.
# The tests passed with caching disabled, but you will need to configure
# this for optimal performance.
REDIS_HOST="localhost"
REDIS_PORT="6379"
REDIS_DB="0"

# -----------------------------------------------------------------------------
# General Application Settings
# -----------------------------------------------------------------------------
# Directory to save results from trading analysis.
TRADINGAGENTS_RESULTS_DIR="./results"